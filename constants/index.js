// constants/index.js
const errorMessages = require('./errorMessages');
const responseStatusCodes = require('./responseStatusCodes');
const config = require('../config/config');

module.exports = {
  errorMessages,
  config,
  responseStatusCodes,
};